ICU - The embedded SOC that manages communication between the host and the IPUs.
BOS - BREAK-On-Sync exception.
lgcta - `libgraphcore_target_access`. The archive holds a large amount of unstripped information on how to communicate with tiles and the IPUs, and invokes `libipu_user` to communicate with the kernel driver.