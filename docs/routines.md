Various routines found around the SDK's various binaries and executables

# libgraphcore_target_access (lgcta)
### initIPUMemory(STRIDE: u8)
```
setzi $m3, TODO
setzi $m4, (TMEM_REGION0_BASE_ADDR / STRIDE)
...
```
